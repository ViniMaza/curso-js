const [a, b, c, d] = [3, 4, 5, 6]

const soma = a + b + c + d
const subtracao = a - b
const multiplicacao = a * c
const divisao = d / b
const resto = c % d

console.log(soma, subtracao, multiplicacao, divisao, resto)