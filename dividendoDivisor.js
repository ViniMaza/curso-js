function divisaoResto(dividendo, divisor){
    let result = dividendo / divisor
    let resultResto = dividendo % divisor
    console.log(`O resultado da divisão é ${result}
O resto da divisão é ${resultResto}`)
}
divisaoResto(10,2)